server:
  port: 8088

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/foodie-shop-dev?useUnicode=true&characterEncoding=UTF-8&autoReconnect=true&useSSL=false
    password: rootroot
  redis:
    database: 1
    host: 172.16.178.138
    port: 6379
    password: hins

mybatis:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl      # 开启控制台打印sql日志 不要在生产环境使用
